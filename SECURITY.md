# 보안 정책

## 취약점 신고

보안 취약점은 **공개 이슈로 올리지 말고** GitHub 의 비공개 신고 경로를 이용해 주세요:

- 저장소 **Security → Report a vulnerability**(GitHub Private Vulnerability Reporting).

가능하면 다음을 포함해 주세요: 영향 범위, 재현 절차, 영향을 받는 버전. 접수 후 합리적인 기간 내에 회신하고, 수정 릴리스와 함께 공개합니다.

## 지원 버전

최신 MINOR 릴리스에 대해 보안 수정을 제공합니다.

| 버전 | 지원 |
|---|---|
| 1.0.x | ✅ |
| < 1.0 | ❌ |

## 설계상 안전장치

- **완전 오프라인**: 질의 시 외부 API 를 호출하지 않습니다(데이터가 외부로 나가지 않음).
- **로컬 자격증명(env 전용)**: 토큰은 환경변수 또는 로컬 `.env` 파일에만 두며 저장소에 커밋하지 않습니다(`.gitignore` 처리). `.env` 는 실행 시 환경변수로만 로드됩니다.
- **CI 시크릿 스캔**: PR 마다 시크릿/개인정보 패턴을 스캔해 유입을 차단합니다.
